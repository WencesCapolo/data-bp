# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/data/*
/data/basket_dashboard_29.html
/data/payments(1).csv
/data/users.csvdump.pgc
dump.pgc
# local agent config (skills are committed, machine-local settings are not)
.claude/settings.local.json

# one-off exports left in the working tree — Provider ledgers, never committed
/payments*.csv
/subscriptions*.csv
/reporte_ligas_*.csv
/public/judicializaciones-*.xlsx
/public/juicios-por-provincia-*.xlsx

# the dashboard prototype — 13,9 MB of real revenue figures, and public/ is served
# unauthenticated. Local-only artifact; the docs/handoff/ files cite it by path.
/public/dashboard.html

# sync-alert firing state (scripts/alert-sync-stale.ts)
.alert-sync-state.json
